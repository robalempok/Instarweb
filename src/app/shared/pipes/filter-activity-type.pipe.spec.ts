import { FilterActivityTypePipe } from './filter-activity-type.pipe';

describe('FilterActivityTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActivityTypePipe();
    expect(pipe).toBeTruthy();
  });
});
