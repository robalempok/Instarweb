import { ButtonShareDirective } from './button-share.directive';

describe('ButtonShareDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonShareDirective(null);
    expect(directive).toBeTruthy();
  });
});
