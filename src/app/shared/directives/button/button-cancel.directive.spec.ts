import { ButtonCancelDirective } from './button-cancel.directive';

describe('ButtonCancelDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonCancelDirective();
    expect(directive).toBeTruthy();
  });
});
